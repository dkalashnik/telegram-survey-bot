services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-phidiary}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: examples/web/Dockerfile
    environment:
      DB_HOST: ${POSTGRES_HOST:-db}
      DB_USER: ${POSTGRES_USER:-postgres}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      DB_NAME: ${POSTGRES_DB:-phidiary}
      DB_PORT: ${POSTGRES_PORT:-5432}
      JWT_SECRET: ${JWT_SECRET:-your_jwt_secret_here}
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy

  bot:
    build:
      context: .
      dockerfile: examples/cmd/bot/Dockerfile
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-your_bot_token_here}
      DB_HOST: ${POSTGRES_HOST:-db}
      DB_USER: ${POSTGRES_USER:-postgres}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      DB_NAME: ${POSTGRES_DB:-phidiary}
      DB_PORT: ${POSTGRES_PORT:-5432}
      API_URL: http://api:8080/api/v1
    depends_on:
      - api

  # web:
  #   build:
  #     context: .
  #     dockerfile: web/therapist/Dockerfile
  #   ports:
  #     - "5173:5173"
  #   volumes:
  #     - ./web/therapist/web/therapist:/app
  #     - /app/node_modules
  #   environment:
  #     VITE_API_URL: ${VITE_API_URL:-http://localhost:8080/api}
  #   depends_on:
  #     - api

volumes:
  postgres_data: